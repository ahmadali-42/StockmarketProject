
public interface Iterator {         // Iteratior design patter ninterface
    
   public boolean hasNext();    // methods must impement in child classes of interface.
   public Object next();
   
}